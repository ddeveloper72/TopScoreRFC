// Production environment configuration template
// IMPORTANT: This is a template file with no secrets.
// All configuration is handled via runtime env.js files.

export const environment = {
  production: true,
  // All actual configuration values are loaded at runtime via env.js
  // This ensures secrets are never committed to the repository
};
